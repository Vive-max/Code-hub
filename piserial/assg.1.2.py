# Python script  to calculate area of circle,radius is taken from user
#area of circle=pi*r*r

from math import *
radius = int(input('Enter a circle of radius'))

area_of_circle = pi*radius*radius
print(area_of_circle)